const controller = {}


controller.index = (request, response) => {
    response.render("vistaInicial");
}

controller.altausu = (request, response) =>{
    //const  = request.body;
}

module.exports = controller;